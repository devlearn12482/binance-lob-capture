# Public market-data capture needs NO credentials.
# These are only placeholders for optional future authenticated endpoints.
# Copy to .env and fill if ever needed. NEVER commit real values.
BINANCE_API_KEY=
BINANCE_API_SECRET=
